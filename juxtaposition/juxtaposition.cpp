#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
    int N, T, M;
    cin >> N >> T >> M;
    int total = N * T * M;
    cout << total << endl;
    return 0;
}
#include <bits/stdc++.h>
using namespace std;

int main() {
	ios_base::sync_with_stdio(false);
	cin.tie(NULL);
	string input;
    cin >> input;
    cout << input << " " << input << " " << input << " " << endl;
    return 0;
}